{
  "name": "massingviser-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The browser layer: a reader for the massingviser-mesh payload format, and a three.js viewer built on it.",
  "license": "MIT",
  "scripts": {
    "test": "node --test test/mvmesh.test.mjs",
    "test:render": "node --test test/render.test.mjs",
    "test:all": "node --test test/*.test.mjs",
    "fixtures": "python test/generate_fixtures.py",
    "vendor": "python vendor/fetch_vendor.py"
  },
  "exports": {
    "./mvmesh": "./src/mvmesh.js"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "puppeteer": "^25.4.0"
  }
}
